[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hygiene"
version = "0.1.0"
description = "Deterministic Rule Hygiene fix-generation engine — no I/O, no LLM"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["hygiene"]
