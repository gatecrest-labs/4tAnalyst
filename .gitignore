# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
*.egg
.eggs/
dist/
build/
.venv/
venv/
env/
.env

# Secrets / environment
.env.local
.env.*.local
*.key
*.pem
credentials.yaml
device_zone_map.yaml

# Real internal zone names (internal mapping of FMG ADOM zones to 4THealth zones)
docs/zone-name-mapping.md

# Real network segmentation data (subnets, zones, site names) — never commit.
# policy_db.json is generated locally from your own TUFIN/USP CSV exports via
# `python standards_mcp/build_policy_db.py`; this repo ships no real data.
standards_mcp/policy_db.json
standards_mcp/policy-data/

# Live-test artifacts (real IPs/hostnames from an actual FortiManager/zone API)
docs/test-results/

# Claude Code local/session state — machine-specific, not project config
.claude/worktrees/
.claude/settings.local.json
# Engineer MCP config contains bearer tokens — never commit
.claude/mcp_servers.json
.mcp.json

# Internal planning docs — not needed for public consumption, may reuse
# real-looking lab data from design sessions
docs/superpowers/

# Working documents (will become README.md)
production.md

# Feedback store (runtime data, not source)
feedback.db
feedback.db-wal
feedback.db-shm

# Web admin runtime data
data/users.json
data/analytics.db

# Editors
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# OS
Thumbs.db

# Generated HTML docs (built from MD sources)
html/

# Per-request engineer deliverables (HTML report + CLI config), attached to
# tickets manually -- not versioned repo content
output/

# graphify knowledge-graph cache (generated, machine-local)
graphify-out/

# Superpowers worktrees (local scratch)
.worktrees/

scripts/sync-to-github.sh
